
$imgUrl = '~@/assets/img/'

// 背景引入图片进行初始化设置
bgImg(img)
  bg_img = $imgUrl + img
  background-image url(bg_img)
  background-size 100% 100%
  background-repeat no-repeat

/* 按钮样式 */
btn()
  cursor pointer
  border-radius 2em
  text-align center
  user-select none